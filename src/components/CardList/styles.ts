import styled from 'styled-components';

export const ContainerCardList = styled.div`
  & p {
    font-family: var(--font-family-secondary);
    font-size: 1.125rem;
    color: #262626;
    margin-top: 0.711rem;
    margin-bottom: 2rem;
    margin: 0.711rem 0 1rem 0.7rem;
  }
`;
